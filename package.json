{
  "scripts": {
    "start": "nodemon --ext md,vue,styl --watch .vuepress --watch . --exec vuepress dev",
    "build": "export NODE_OPTIONS=--openssl-legacy-provider && vuepress build"
  },
  "devDependencies": {
    "nodemon": "^2.0.16",
    "vuepress": "^1.9.7"
  }
}
